# Local functions

[ -e ~/.config/shell/functions.local ] && source ~/.config/shell/functions.local

geoip() {
    curl "https://ipvigilante.com/$1"
}

cmkd() {
    mkdir "$1"
    cd "$1"
}

cl() {
    cd "$1"
    ls
}
